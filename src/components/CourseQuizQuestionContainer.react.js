const React = require('react');

const CourseQuizQuestionContainer = require('./CourseQuizQuestionViewer.react');

class CourseQuizQuestionContainer extends React.Component {
  constructor(props) {
    super(props);
  }

  render() {
    return (
      <div>
        <span />
      </div>
    );
  }
}

module.exports = CourseQuizQuestionContainer;